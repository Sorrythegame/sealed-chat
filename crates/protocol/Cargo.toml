[package]
name = "sealed-protocol"
version = "0.1.2"
edition = "2021"
license = "MIT"
description = "Shared serde types and API DTOs for sealed-chat."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
base64 = { workspace = true }
chrono = { workspace = true }
